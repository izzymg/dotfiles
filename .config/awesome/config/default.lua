-- Configuration for Izzy's awesome wm rc

local isa_config = {
    terminal = "urxvt",
    modkey = "Mod4",
    -- Appended to dmenu_run
    dmenu_string = "-fn 'leggie-8' -nb '#1b1b1b' -nf '#fff' -sb '#bf6976' -h 30 -b -s 0 -dim 0.5",
}

return isa_config